Checkbox for multi-select and consent.

```jsx
<Checkbox defaultChecked label="Đồng ý nhận bản tin" />
<Checkbox checked={v} onChange={setV} />
```

Controlled (`checked` + `onChange`) or uncontrolled (`defaultChecked`). Optional `label` to the right.
