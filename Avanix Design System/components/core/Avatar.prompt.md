Circular avatar for members, authors, and the brand.

```jsx
<Avatar src="/assets/logo/avatar.png" name="Avanix" verified ring size={56} />
<Avatar name="Nguyên Pie" />   {/* initials fallback */}
```

`size` in px, `ring` for the electric-blue outline, `verified` for the check badge. Without `src` it shows initials on a blue→navy gradient.
